<Window x:Class="UniversalVideoDownloader.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification" Height="80" Width="320"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">
    <Border Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="8" Margin="4" Padding="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Color="#020617" Opacity="0.5" ShadowDepth="2"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Success/Failure Icon -->
            <Ellipse Grid.Column="0" Width="36" Height="36" Margin="0,0,12,0" VerticalAlignment="Center">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="#10B981"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                                <Setter Property="Fill" Value="#EF4444"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            
            <TextBlock Grid.Column="0" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,12,0" 
                       FontSize="18" 
                       Foreground="White" 
                       FontWeight="Bold">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="&#x2714;"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                                <Setter Property="Text" Value="&#x274C;"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Message details -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding TitleText}" FontWeight="Bold" Foreground="White" FontSize="12" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding MessageText}" Foreground="#94A3B8" FontSize="11" Margin="0,2,0,0"/>
            </StackPanel>

            <!-- Dismiss -->
            <Button Grid.Column="2" 
                    Content="&#x2715;" 
                    Click="Close_Click" 
                    Background="Transparent" 
                    Foreground="#64748B" 
                    BorderThickness="0" 
                    Cursor="Hand" 
                    VerticalAlignment="Top" 
                    HorizontalAlignment="Right" 
                    FontSize="12"/>
        </Grid>
    </Border>
</Window>
